apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: srijaldocker/node-app:frontend-marchh1
        ports:
        - containerPort: 80
        env:
        - name: REACT_APP_BACKEND_API_URL
          value: "http://a7d767d2c13a74c3db9e20a88be17cbd-40707563.ap-southeast-1.elb.amazonaws.com:5000/api"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 80
  type: LoadBalancer

